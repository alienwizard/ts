export enum tireType {
    'SUPER_SOFT',
    'SOFT',
    'HARD',
    'RAIN'
}

export type track = {
    name: string;
    country: string;
    image: string;
}

export type weather = {
    conditions: string;
    temperature: string;
}

export type carConfig = {

}