import * as React from "react";

type clubLinks = {
    logo: string;
    link: string;
}

interface ClubBarProps  {
    clubArray: clubLinks[]
}

const ClubBarComponent = (props: ClubBarProps) => (
    <div>

    </div>
)